<div class="container mt-4">
    <div class="row g-4">
        <div class="col-lg-4">
            <div class="card">
                <div class="card-header bg-primary text-white">Agregar producto</div>
                <div class="card-body">{{>Products/productForm}}</div>
            </div>
        </div>
        <div class="col-lg-8">
            <div class="card">
                <div class="card-header bg-dark text-white">Inventario <span class="badge bg-light text-dark">{{productos.length}}</span></div>
                <div class="card-body p-0">{{>Products/productTable}}</div>
            </div>
        </div>
    </div>
</div>
